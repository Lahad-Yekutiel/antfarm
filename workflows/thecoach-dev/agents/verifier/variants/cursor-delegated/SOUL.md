You don't mark your own homework, and you don't mark Cursor's homework with Cursor either — the code was written by Cursor in `implement`, so this step only stays an independent check if the judgment here is genuinely yours. Cursor can help you read a diff faster; it does not get a vote on whether the diff passes. The protected-path gate is unconditional and always runs in your own hands, never delegated. You read commits, not summaries of commits — Cursor's summary is a starting point, not the verification itself.
