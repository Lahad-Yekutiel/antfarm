STATUS: done
CHANGES: Delegated to Cursor CLI via /delegate. Cursor created tools/schema-drift/ (standalone, outside npm workspaces list: apps/*, packages/*): tsconfig.json (strict, NodeNext module/moduleResolution, allowImportingTsExtensions, include: *.mts); parse-migrations.mts exporting parseMigrations(directoryPath) which reads .sql files in sorted filename order, strips -- line comments and quoted-string content correctly, matches CREATE TABLE public.<name> (with optional IF NOT EXISTS) case-insensitively via a regex built from named constants (SQL_FILE_EXTENSION, PUBLIC_SCHEMA_PREFIX), tracks paren depth (respecting quotes) to find the matching close-paren across multi-line bodies, splits top-level comma-separated items, and excludes constraint-only lines (unique(, check(, primary key(, foreign key(, constraint ...) from the returned column list while still stripping default/check/references modifiers off real column lines; returns Map<string, {kind:'table', columns:string[]}>; parse-migrations.test.mts (node:test) with two fixture SQL files under fixtures/ (001_profiles_trainers.sql: profiles + trainers tables with default, named constraint+check; 002_workout_exercises.sql: create index line, workout_exercises table with inline check clauses and two multi-column unique(...) constraint lines) asserting correct table names/column lists and that create index / comment lines produce no spurious table entries. I independently verified: git status/diff confirmed nothing under _SSoT/**, supabase/**, .github/workflows/**, or .gitignore was touched, and root package.json workspaces list is unchanged (tools/ remains outside it); reviewed all 5 new files by hand for correctness against the acceptance criteria; ran the two verification commands myself from repo root (not just trusting Cursor's self-report). Committed as 9e98c16.
TESTS: Ran myself, independently, from repo root: `npx tsc --noEmit -p tools/schema-drift/tsconfig.json` -> exit 0, zero errors. `node --test tools/schema-drift/parse-migrations.test.mts` -> both tests passed (tests 2, pass 2, fail 0): "parseMigrations extracts table names and column lists from migration SQL files" and "parseMigrations ignores create index statements and comment-only lines". COMMIT_SHA: 9e98c16