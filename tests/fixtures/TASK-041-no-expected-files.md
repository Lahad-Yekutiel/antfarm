# TASK-041: No Expected files fixture for dispatch scope-gate tests

## Status
Ready

## Objective
Synthetic fixture — no ## Expected files section, so the scope gate must skip.

## Branch
`feature/no-expected-files-fixture` — cut from `staging`. Never `main`.

## Tool/model
Cursor
