# OpenClaw sandbox.docker hard limits — 2026-08-28
# Live source: ~/.openclaw/openclaw.json
# Applied to:
#   agents.defaults.sandbox.docker
#   agents.list[thecoach-dev_setup].sandbox.docker
#   agents.list[thecoach-dev_developer].sandbox.docker
#
# Existing containers do NOT pick this up; they must be recreated.
# Schema keys (openclaw SandboxDockerSettings): memory, memorySwap, cpus, pidsLimit.
# Backup of live config: ~/.openclaw/openclaw.json.bak-2026-08-28-sandbox-limits

memory=3g
memorySwap=3g
cpus=4
pidsLimit=512
